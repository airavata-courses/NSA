apiVersion: v1
kind: PersistentVolume
metadata:
  name: pp-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/postprocess"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pp-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
